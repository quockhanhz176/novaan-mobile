import React, { type FC } from "react";
import { Text } from "react-native";

const WarningAsterisk: FC = () => {
    return <Text className="text-cwarning">*</Text>;
};

export default WarningAsterisk;
